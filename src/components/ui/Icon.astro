---
const { name } = Astro.props;

const icons = import.meta.glob("../../assets/icons/about/app/*.svg", {
  eager: true,
  import: "default",
}) as Record<string, any>;

const Svg = icons[`../../assets/icons/about/app/${name}.svg`];
---

{Svg && <Svg class="icon"/>}
