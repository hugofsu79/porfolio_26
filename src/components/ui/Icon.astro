---
const { name } = Astro.props;

const icons = import.meta.glob("/public/icons/about/app/*.svg", {
  eager: true,
  import: "default",
}) as Record<string, any>;

const Svg = icons[`/public/icons/about/app/${name}.svg`];
---

{Svg && <Svg class="icon" />}
